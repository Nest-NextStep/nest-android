<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Nest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorPrimaryVariant">@color/purple_30</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/gray</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="bottomNavigationStyle">@style/Widget.App.BottomNavigationView</item>
    </style>

    <style name="Widget.App.BottomNavigationView" parent="Widget.Material3.BottomNavigationView">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.BottomNavigationView</item>
    </style>

    <style name="ThemeOverlay.App.BottomNavigationView" parent="">
        <item name="colorSurfaceContainer">@color/black</item>

        <!-- Active indicator color -->
        <item name="colorSecondaryContainer">@color/black</item>

        <!-- Active icon and label color -->
        <item name="colorOnSecondaryContainer">?attr/colorPrimary</item>
        <item name="colorOnSurface">?attr/colorPrimary</item>

        <!-- Inactive icon and label color -->
        <item name="colorOnSurfaceVariant">?attr/colorSecondary</item>
    </style>

</resources>